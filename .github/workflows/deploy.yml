#name: Deploy to GitHub Pages
#
#on:
#  push:
#    branches:
#      - main
#  workflow_dispatch:
#
#permissions:
#  contents: read
#  pages: write
#  id-token: write
#
#concurrency:
#  group: "pages"
#  cancel-in-progress: false
#
#jobs:
#  build:
#    runs-on: macos-15
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Install Swift 6.2
#        run: |
#          curl -L https://download.swift.org/swift-6.2.3-release/xcode/swift-6.2.3-RELEASE/swift-6.2.3-RELEASE-osx.pkg -o swift.pkg
#          sudo installer -pkg swift.pkg -target /
#          export PATH="/Library/Developer/Toolchains/swift-6.2.3-RELEASE.xctoolchain/usr/bin:$PATH"
#          echo "/Library/Developer/Toolchains/swift-6.2.3-RELEASE.xctoolchain/usr/bin" >> $GITHUB_PATH
#
#      - name: Install SwiftWasm SDK
#        run: |
#          curl -L --retry 3 --retry-delay 5 https://download.swift.org/swift-6.2.3-release/wasm-sdk/swift-6.2.3-RELEASE/swift-6.2.3-RELEASE_wasm.artifactbundle.tar.gz -o swift-wasm-sdk.tar.gz
#          swift sdk install ./swift-wasm-sdk.tar.gz --checksum 394040ecd5260e68bb02f6c20aeede733b9b90702c2204e178f3e42413edad2a
#
#      - name: Setup Node.js
#        uses: actions/setup-node@v4
#        with:
#          node-version: '20'
#
#      - name: Resolve Swift packages
#        run: swift package resolve
#
#      - name: Install dependencies
#        run: npm install --ignore-scripts
#
#      - name: Build with Vite
#        run: npm run build
#        env:
#          GITHUB_PAGES: true
#
#      - name: Upload artifact
#        uses: actions/upload-pages-artifact@v3
#        with:
#          path: ./dist
#
#  deploy:
#    environment:
#      name: github-pages
#      url: ${{ steps.deployment.outputs.page_url }}
#    runs-on: ubuntu-latest
#    needs: build
#    steps:
#      - name: Deploy to GitHub Pages
#        id: deployment
#        uses: actions/deploy-pages@v4
